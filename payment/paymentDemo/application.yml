spring:
   docker:
      compose:
         enabled: false
   rabbitmq:
      host: rabbitmq
      port: 5672
      username: guest
      password: guest
      

server:
   port: 8006
springdoc:
   api-docs:
      enabled: true
   swagger-ui:
      enabled: true
      path: /swagger-ui.html